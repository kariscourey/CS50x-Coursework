#include <stdio.h>

int main(void)
{
    // int n = get_int("How many scores? ")
    // double average; 

    // for (i = 0; i<n; i++)
    // {
    //     int score[i] = get_int("Score: ");

    //     average+=i;

    // }
    
    // average=/n;

    int n = get_int("How many scores? ")

    int scores[n];

    for (i = 0, i < n; i++)
    {
        scores[i] = get_int("Score: ")'';
        int scoresum = scores[i]++;
    }

    float scoreaverage = scoresum/(float) n;



}